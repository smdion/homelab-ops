<!-- AUTO-GENERATED by generate_docs.py on 2026-03-06 15:25 — do not edit manually -->

# Quick Reference

All playbook templates at a glance — what they do and what extra variables they accept.

## Backup

| Template | Description | Required | Optional |
|---|---|---|---|
| Backup — AMP [Config] | Back up host configurations and appdata. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>`, `with_databases=yes` |
| Backup — B2 [Offsite] | Sync local backups to Backblaze B2 offsite storage. | — | `offsite_method=restic|rclone`, `dry_run=yes`, `bwlimit=<rate>` |
| Backup — Database [Primary InfluxDB] | Back up databases from remote Docker containers. | `hosts_variable` | — |
| Backup — Database [Primary MariaDB] | Back up databases from remote Docker containers. | `hosts_variable` | — |
| Backup — Database [Primary PostgreSQL] | Back up databases from remote Docker containers. | `hosts_variable` | — |
| Backup — Database [Secondary PostgreSQL] | Back up databases from remote Docker containers. | `hosts_variable` | — |
| Backup — Docker Run [Appdata] | Back up host configurations and appdata. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>`, `with_databases=yes` |
| Backup — Docker Stacks [Appdata] | Back up host configurations and appdata. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>`, `with_databases=yes` |
| Backup — Docker Stacks [Combined] | Back up host configurations and appdata. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>`, `with_databases=yes` |
| Backup — PiKVM [Config] | Back up host configurations and appdata. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>`, `with_databases=yes` |
| Backup — Proxmox [Config] | Back up host configurations and appdata. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>`, `with_databases=yes` |
| Backup — Unifi Network [Config] | Back up host configurations and appdata. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>`, `with_databases=yes` |
| Backup — Unifi Protect [Config] | Back up host configurations and appdata. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>`, `with_databases=yes` |
| Backup — unRAID [Config] | Back up host configurations and appdata. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>`, `with_databases=yes` |
| Backup — unRAID [Offline] | Sync backups to offline Synology storage. | `hosts_variable` | `synology_skip_shutdown=true`, `synology_share_name=<name>` |

## Restore

| Template | Description | Required | Optional |
|---|---|---|---|
| Restore — AMP [Instance] | Restore AMP game server instance(s) from backup. | `restore_target`, `confirm=yes` | `amp_instance_filter` |
| Restore — App [Production] | Restore a single app (stack appdata + databases) to a production host. | `restore_app`, `restore_target`, `confirm=yes` | — |
| Restore — Database [Primary InfluxDB] | Restore database dumps from backup. | `hosts_variable`, `confirm=yes` | `restore_db=<name>`, `restore_date=YYYY-MM-DD`, `skip_pre_backup=yes` |
| Restore — Database [Primary MariaDB] | Restore database dumps from backup. | `hosts_variable`, `confirm=yes` | `restore_db=<name>`, `restore_date=YYYY-MM-DD`, `skip_pre_backup=yes` |
| Restore — Database [Primary PostgreSQL] | Restore database dumps from backup. | `hosts_variable`, `confirm=yes` | `restore_db=<name>`, `restore_date=YYYY-MM-DD`, `skip_pre_backup=yes` |
| Restore — Database [Secondary PostgreSQL] | Restore database dumps from backup. | `hosts_variable`, `confirm=yes` | `restore_db=<name>`, `restore_date=YYYY-MM-DD`, `skip_pre_backup=yes` |
| Restore — Docker Run [Appdata] | Restore config/appdata archives from backup. | `hosts_variable`, `confirm=yes` | `stack=<name>`, `app=<name>`, `role=<name>`, `restore_app=<name>`, `with_databases=yes` |
| Restore — Docker Stacks [Appdata] | Restore config/appdata archives from backup. | `hosts_variable`, `confirm=yes` | `stack=<name>`, `app=<name>`, `role=<name>`, `restore_app=<name>`, `with_databases=yes` |
| Restore — Proxmox [Config] | Restore config/appdata archives from backup. | `hosts_variable`, `confirm=yes` | `stack=<name>`, `app=<name>`, `role=<name>`, `restore_app=<name>`, `with_databases=yes` |
| Restore — unRAID [Config] | Restore config/appdata archives from backup. | `hosts_variable`, `confirm=yes` | `stack=<name>`, `app=<name>`, `role=<name>`, `restore_app=<name>`, `with_databases=yes` |

## Deploy

| Template | Description | Required | Optional |
|---|---|---|---|
| Deploy — Docker Stacks [Validate] | Deploy Docker stacks from Git to target hosts. | `hosts_variable` | `stack=<name>`, `app=<name>`, `deploy_stack=<name>`, `role=<name>`, `dry_run=true`, `pull_only=yes`, `debug_no_log=yes` |
| Deploy — Docker [Stacks] | Deploy Docker stacks from Git to target hosts. | `hosts_variable` | `stack=<name>`, `app=<name>`, `deploy_stack=<name>`, `role=<name>`, `dry_run=true`, `pull_only=yes`, `debug_no_log=yes` |
| Deploy — Docs [Site] | Deploy homelab-docs MkDocs site to the docs container on the dev VM. | `hosts_variable` | `stack=<name>`, `role=<name>` |
| Deploy — Grafana [Dashboard] | Deploy Grafana dashboard and Ansible-Logging datasource via API. | — | — |
| Deploy — SWAG [Configs] | Generate SWAG reverse proxy site-confs from definitions-as-code. | — | `dry_run=true` |

## Build & Provision

| Template | Description | Required | Optional |
|---|---|---|---|
| Build — AMP [Game Server] | Bootstrap AMP game server: provision VM, install software, restore instances | `confirm=yes` | `vm_state=absent`, `restore_host` |
| Build — PVE [Cluster Join] | Post-install PVE bootstrap — configures a freshly installed Proxmox VE host. | `pve_host` | `cluster_join=yes`, `cluster_master`, `pve_debian_release` |
| Build — PVE [Standalone] | Post-install PVE bootstrap — configures a freshly installed Proxmox VE host. | `pve_host` | `cluster_join=yes`, `cluster_master`, `pve_debian_release` |
| Build — Ubuntu [Apps] | Unified VM lifecycle orchestrator for Proxmox. | `vm_name` | `vm_state=absent`, `vm_state=snapshot`, `vm_state=revert`, `snapshot_name`, `confirm=yes`, `role=core|apps|dev`, `vm_cleanup=no`, `restore_app=<name>`, `deploy_ssh_key=yes`, `skip_dbs=<csv>`, `stack=<name>` |
| Build — Ubuntu [Core] | Unified VM lifecycle orchestrator for Proxmox. | `vm_name` | `vm_state=absent`, `vm_state=snapshot`, `vm_state=revert`, `snapshot_name`, `confirm=yes`, `role=core|apps|dev`, `vm_cleanup=no`, `restore_app=<name>`, `deploy_ssh_key=yes`, `skip_dbs=<csv>`, `stack=<name>` |
| Build — Ubuntu [Desktop] | Unified VM lifecycle orchestrator for Proxmox. | `vm_name` | `vm_state=absent`, `vm_state=snapshot`, `vm_state=revert`, `snapshot_name`, `confirm=yes`, `role=core|apps|dev`, `vm_cleanup=no`, `restore_app=<name>`, `deploy_ssh_key=yes`, `skip_dbs=<csv>`, `stack=<name>` |
| Build — Ubuntu [Dev] | Unified VM lifecycle orchestrator for Proxmox. | `vm_name` | `vm_state=absent`, `vm_state=snapshot`, `vm_state=revert`, `snapshot_name`, `confirm=yes`, `role=core|apps|dev`, `vm_cleanup=no`, `restore_app=<name>`, `deploy_ssh_key=yes`, `skip_dbs=<csv>`, `stack=<name>` |
| Build — Ubuntu [VM] | Unified VM lifecycle orchestrator for Proxmox. | `vm_name` | `vm_state=absent`, `vm_state=snapshot`, `vm_state=revert`, `snapshot_name`, `confirm=yes`, `role=core|apps|dev`, `vm_cleanup=no`, `restore_app=<name>`, `deploy_ssh_key=yes`, `skip_dbs=<csv>`, `stack=<name>` |
| Setup — Ansible User [SSH] | Set up the ansible user on all managed hosts. | — | — |
| Setup — PVE [VIP] | Install and configure keepalived on all Proxmox nodes to provide a floating management VIP. | — | — |
| Setup — Test VLAN [Unifi] | One-time setup for test VM VLAN isolation on Unifi UDM. | — | — |
| Test — Restore [Apps] | Unified VM lifecycle orchestrator for Proxmox. | `vm_name` | `vm_state=absent`, `vm_state=snapshot`, `vm_state=revert`, `snapshot_name`, `confirm=yes`, `role=core|apps|dev`, `vm_cleanup=no`, `restore_app=<name>`, `deploy_ssh_key=yes`, `skip_dbs=<csv>`, `stack=<name>` |
| Test — Restore [Core] | Unified VM lifecycle orchestrator for Proxmox. | `vm_name` | `vm_state=absent`, `vm_state=snapshot`, `vm_state=revert`, `snapshot_name`, `confirm=yes`, `role=core|apps|dev`, `vm_cleanup=no`, `restore_app=<name>`, `deploy_ssh_key=yes`, `skip_dbs=<csv>`, `stack=<name>` |
| Test — Restore [Dev] | Unified VM lifecycle orchestrator for Proxmox. | `vm_name` | `vm_state=absent`, `vm_state=snapshot`, `vm_state=revert`, `snapshot_name`, `confirm=yes`, `role=core|apps|dev`, `vm_cleanup=no`, `restore_app=<name>`, `deploy_ssh_key=yes`, `skip_dbs=<csv>`, `stack=<name>` |

## Maintain

| Template | Description | Required | Optional |
|---|---|---|---|
| Maintain — AMP [Cleanup] | AMP Server Maintenance Playbook | — | `amp_scope=versions|cleanup|coredumps|prune|journal`, `amp_versions_keep=<N>` |
| Maintain — Docker [Cleanup] | Docker prune + page cache maintenance. | — | — |
| Maintain — Guacamole [Connections] | Manage Guacamole connections and user group permissions declaratively. | — | `remove_unmanaged=yes`, `confirm=yes` |
| Maintain — Health [Check] | Health Maintenance Playbook | — | — |
| Maintain — Logging DB [Cleanup] | Purge failure and stale records from ansible_logging. | — | `purge_all=yes`, `confirm=yes` |
| Maintain — Logging DB [Purge All] | Purge failure and stale records from ansible_logging. | — | `purge_all=yes`, `confirm=yes` |
| Maintain — PVE [Config] | Idempotent Proxmox node configuration — run after bare-metal reinstall or new node join. | — | — |
| Maintain — Semaphore [Cleanup] | Clean up Semaphore task history and prune ansible_logging retention. | — | — |
| Maintain — Unifi [Restart] | Restart Unifi Network services. | — | — |

## Test & Verify

| Template | Description | Required | Optional |
|---|---|---|---|
| Test — Backup Restore [VM] | Test all app restores on a disposable VM. | `source_host` | `vm_name`, `vm_index`, `test_apps`, `deploy_ssh_key` |
| Verify — AMP [Backup] | Verify that the latest backups are restorable. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>` |
| Verify — Database [Primary InfluxDB] | Verify that the latest backups are restorable. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>` |
| Verify — Database [Primary MariaDB] | Verify that the latest backups are restorable. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>` |
| Verify — Database [Primary PostgreSQL] | Verify that the latest backups are restorable. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>` |
| Verify — Database [Secondary PostgreSQL] | Verify that the latest backups are restorable. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>` |
| Verify — Docker Run [Appdata] | Verify that the latest backups are restorable. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>` |
| Verify — Docker Stacks [Appdata] | Verify that the latest backups are restorable. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>` |
| Verify — Proxmox [Config] | Verify that the latest backups are restorable. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>` |
| Verify — VLAN [Isolation] | Lightweight test VLAN isolation verification. | `vm_name` | — |
| Verify — unRAID [Config] | Verify that the latest backups are restorable. | `hosts_variable` | `stack=<name>`, `app=<name>`, `role=<name>` |

## Update

| Template | Description | Required | Optional |
|---|---|---|---|
| Rollback — Docker [Containers] | Rollback Docker containers to their previous image versions. | `hosts_variable`, `confirm=yes` | `stack=<name>`, `app=<name>`, `rollback_stack=<name>`, `rollback_service=<name>`, `role=<name>`, `with_backup=yes` |
| Update — AMP [Instances] | Update OS packages, Docker containers, and unRAID plugins. | `hosts_variable` | `config_file`, `update_scope=os|docker|software|plugin` |
| Update — Docker Run [Containers] | Update OS packages, Docker containers, and unRAID plugins. | `hosts_variable` | `config_file`, `update_scope=os|docker|software|plugin` |
| Update — Docker Stacks [Containers] | Update OS packages, Docker containers, and unRAID plugins. | `hosts_variable` | `config_file`, `update_scope=os|docker|software|plugin` |
| Update — PiKVM [Appliance] | Update OS packages, Docker containers, and unRAID plugins. | `hosts_variable` | `config_file`, `update_scope=os|docker|software|plugin` |
| Update — Proxmox [Appliance] | Update OS packages, Docker containers, and unRAID plugins. | `hosts_variable` | `config_file`, `update_scope=os|docker|software|plugin` |
| Update — Ubuntu [OS] | Update OS packages, Docker containers, and unRAID plugins. | `hosts_variable` | `config_file`, `update_scope=os|docker|software|plugin` |
| Update — unRAID [Plugins] | Update OS packages, Docker containers, and unRAID plugins. | `hosts_variable` | `config_file`, `update_scope=os|docker|software|plugin` |

## Utilities

| Template | Description | Required | Optional |
|---|---|---|---|
| Apply — Full [All] | Full reconciliation — apply roles, regenerate SWAG proxy configs, sync Guacamole connections. | — | `role=core`, `stack=auth`, `dry_run=true` |
| Apply — Role [All] | Idempotent host state reconciliation — single command to make a host match its definition. | — | `role`, `deploy_ssh_key=yes`, `debug_no_log=yes`, `dry_run=true`, `resize=yes`, `stack=<name>`, `skip_bootstrap=yes`, `skip_stacks=yes`, `skip_verify=yes` |
| DR — Rebuild [Apps] | DR rebuild — build a VM and restore stack backups in one command. | `role`, `confirm=yes` | `vm_name`, `deploy_ssh_key=yes`, `skip_dbs`, `restore_app`, `vm_index=N`, `debug_no_log=yes` |
| DR — Rebuild [Core] | DR rebuild — build a VM and restore stack backups in one command. | `role`, `confirm=yes` | `vm_name`, `deploy_ssh_key=yes`, `skip_dbs`, `restore_app`, `vm_index=N`, `debug_no_log=yes` |
| DR — Rebuild [Dev] | DR rebuild — build a VM and restore stack backups in one command. | `role`, `confirm=yes` | `vm_name`, `deploy_ssh_key=yes`, `skip_dbs`, `restore_app`, `vm_index=N`, `debug_no_log=yes` |
| Download — Videos [Channels] | Download videos via MeTube / yt-dlp. | `hosts_variable`, `config_file` | — |
| Download — Videos [On Demand] | Download videos via MeTube / yt-dlp. | `hosts_variable`, `config_file` | — |
| Maintain — Logging DB [Summary] | Weekly ansible_logging summary — queries 7-day row counts, hosts with no recent backup, and failed operation counts; sends a notification and logs to MariaDB. | — | — |
| Manage — Vault | Vault management utility — check status, decrypt, encrypt, change, or validate secrets.yaml. | — | `action=status|change|decrypt|encrypt|validate`, `vault_pass_file=<path>` |
| Purge — Ceph [PVE] | One-time complete Ceph removal from all PVE cluster nodes. | `confirm=yes` | — |
| Review — Proxmox [Cluster+PBS] | Read-only review of Proxmox VE cluster and PBS backup server state. | — | — |
| Test — Cleanup [VMs] | Remove all test VMs from the Proxmox cluster. | `confirm=yes` | — |

