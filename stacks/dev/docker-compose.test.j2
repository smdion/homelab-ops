# Test-mode compose override — replaces NFS volumes with local empty dirs.
# Applied automatically by deploy_single_stack.yaml when _test_mode is true.
# code-server healthcheck (ls /workspace/odyssey /workspace/vps) passes:
# mountpoints exist as empty local dirs.
volumes:
  odyssey:
    driver: local
  vps:
    driver: local
