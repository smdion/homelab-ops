# Test-mode compose override — replaces NFS bind mounts with local empty dirs.
# Applied automatically by deploy_single_stack.yaml when _test_mode is true.
# code-server healthcheck (ls /workspace/core /workspace/apps /workspace/dev /workspace/vps /workspace/homelab-ops) passes:
# mountpoints exist as empty local dirs.
volumes:
  core:
    driver: local
  apps:
    driver: local
  dev:
    driver: local
  vps:
    driver: local
  homelab-ops:
    driver: local
