x-base: &base
  mem_limit: ${MEM_LIMIT}
  restart: ${RESTART}
x-env: &env
  TZ: ${TZ}
services:
  nfs:
    <<: *base
    container_name: nfs
    image: itsthenetwork/nfs-server-alpine:latest
    network_mode: host
    environment:
      <<: *env
      SHARED_DIRECTORY: ${NFS_SHARED_DIRECTORY}
      SYNC: ${NFS_SYNC}
    volumes:
      - ${APPSDIR}:/nfsshare
    privileged: true
